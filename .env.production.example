ENVIRONMENT=production

# generate: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
POSTGRES_PASSWORD=

SESSION_HOURS=12
COOKIE_SECURE=true
CORS_ORIGINS=https://relaydesk.us
APP_BASE_URL=https://relaydesk.us

LDAP_ENABLED=false

SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=Relay Desk
# SAFETY: keep this narrow. "*" allows every recipient.
SMTP_ALLOWLIST=
